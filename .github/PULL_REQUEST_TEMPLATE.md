## Summary

<!-- What does this change do, and why? -->

## Checklist

- [ ] Tests pass locally (`swift test` for the affected package(s))
- [ ] `swiftformat . --lint` and `swiftlint lint` report clean, where applicable
- [ ] `Docs/` updated if user-facing behavior changed
- [ ] `Backend/Public/openapi.yaml` updated if the `/api/v1/` surface changed
- [ ] This PR is expected to pass the relevant CI job(s) (`backend` / `apple` / `extension`)
