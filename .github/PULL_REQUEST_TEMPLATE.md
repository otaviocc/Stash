## Summary

<!-- What does this change do, and why? -->

## Checklist

- [ ] Tests pass locally (`swift test` for the affected package(s))
- [ ] `swiftformat . --lint` and `swiftlint lint` report clean, where applicable
- [ ] `Backend/Public/openapi.yaml` updated if the `/api/v1/` surface changed
- [ ] Product behavior change recorded in the relevant `Docs/product-*.md` (and its `PRODUCT.md` index entry, if needed)
- [ ] Design decision / deviation / trade-off recorded in the relevant `Docs/decisions-*.md` (and its `DECISIONS.md` index entry)
- [ ] Other user-facing docs under `Docs/` updated if applicable
- [ ] This PR is expected to pass the relevant CI job(s) (`backend` / `apple` / `extension`)

<!--
If you used an AI coding agent to help with this PR, make sure it followed the
repository conventions documented in AGENTS.md.
-->
